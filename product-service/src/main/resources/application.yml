server:
  port: 8001

spring:
  application:
    name: product-service

  # RabbitMQ
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  # 数据源
  datasource:
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/stms
    hikari:
      maximum-pool-size: 10

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/

mybatis-plus:
  type-enums-package: com.carlosli.sudoker.product.constant